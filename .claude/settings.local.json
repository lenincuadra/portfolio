{
  "permissions": {
    "allow": [
      "Bash(git --no-pager diff -- cases/case-v3.html styles/case.css styles/tokens.css)",
      "Bash(awk '/^@@ -143,9/{f=1} f{print} /^@@ -173,6/{if\\(f\\)exit}' /tmp/cv3.full.patch)",
      "Bash(git commit -m 'Case images: silent on-brand placeholder fallback' -m 'Every case image slot shows the image when it loads and degrades to a *)",
      "Bash(git commit -m 'Theme: respect OS preference on case pages, fix load flash' -m 'Case pages only honored a stored theme, so a home shown dark via the OS *)",
      "Bash(grep -nE \"^\\\\s{2,6}'[a-z0-9-]+':\\\\s*\\\\{|cases:\\\\s*\\\\{|heroTitle|hero_title|pageTitle|page_title\" data/content.js)"
    ]
  }
}
